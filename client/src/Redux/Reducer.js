
let initialState ={
    sermons: [],

}

function rootReducer(state = initialState, {type, payload}){
    switch (type) {
        case value:
            
    
        default:
            return{
                ...state
            }
    }
}